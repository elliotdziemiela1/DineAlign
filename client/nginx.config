server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  location / {
    # First try to serve the file, then fall back to index.html
    try_files $uri /index.html;
  }
}