{
  "name": "dine_align",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "concurrently": "^9.1.0"
  },
  "scripts": {
    "dev": "concurrently \"npm start --prefix client\" \"npm start --prefix server\"",
    "build": "./startapp.sh"
  }
}
